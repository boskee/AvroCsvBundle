<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block avro_csv_title %}{% endblock avro_csv_title %}</title>
    </head>
    <body>
        {% for type, messages in app.session.flashbag.all() %}
            {% for message in messages %}
                <div class="flash-{{ type }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}

        <div>
            {% block avro_csv_content %}
            {% endblock avro_csv_content %}
        </div>
    </body>
</html>